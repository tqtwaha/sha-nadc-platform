# @sha-nadc/mobile — EMT field app

Phase 7 deliverable. Will be initialized with:

```bash
cd apps/mobile
npx create-expo-app . --template tabs
# then: EAS Build setup, expo-router config, link to @sha-nadc/types + @sha-nadc/domain
```

Native capabilities required (per TRACKER.md):
- Offline-first storage + retry queue
- Background GPS reporting
- Push notifications
- Camera (ePCR photos)
- Bluetooth medical-device pairing (stub OK for MVP)
